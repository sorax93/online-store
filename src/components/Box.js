import styled from "styled-components";
import {
  color,
  space,
  layout,
  flexbox,
  grid,
  background,
  border,
  shadow,
  position,
  typography,
} from "styled-system";

export const Box = styled("div")(
  color,
  space,
  layout,
  flexbox,

  typography,

  grid,
  background,
  border,
  position,
  shadow
);
