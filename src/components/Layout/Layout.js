import AppBar from "../AppBar/AppBar";
import { Box } from "../Box";
import { Main } from "../Main/Main";
export default function Layout() {
  return (
    <Box>
      <AppBar />
      <Main />
    </Box>
  );
}
